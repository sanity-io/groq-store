import {getJsonStream} from '../src/browser/ndjsonStreamer'
import {projectId, dataset} from './config'

describe('can stream', () => {
  it('works', async () => {
    expect(documents).toBeGreaterThan(0)
  })
})
